# ─────────────────────────────────────────────────────────────────────────────
# Production requirements — Hugging Face Spaces (Linux CPU)
# CPU-only PyTorch: ~700 MB instead of ~2.6 GB, fits in HF free tier
# ─────────────────────────────────────────────────────────────────────────────
flask>=3.0.0
gunicorn>=21.2.0
transformers>=4.51.0
# CPU-only PyTorch — smaller, faster to install on Linux
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0
accelerate>=0.27.0
python-docx>=1.1.0
nltk>=3.8.0
huggingface_hub>=0.21.0
